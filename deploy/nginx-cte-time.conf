map $http_x_forwarded_proto $should_https_redirect {
    default          1;
    ""               1;
    ~*^https(,|$)   0;
}

server {
    listen 80;
    server_name cte-time.example.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name cte-time.example.com;

    ssl_certificate /etc/letsencrypt/live/cte-time.example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/cte-time.example.com/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 1d;
    ssl_session_tickets off;

    client_max_body_size 1M;

    proxy_connect_timeout 30s;
    proxy_read_timeout 30s;
    proxy_send_timeout 30s;

    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header X-XSS-Protection "0" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline'" always;

    if ($should_https_redirect) {
        return 301 https://$host$request_uri;
    }

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# WARNING: X-Forwarded-For spoofing is possible in dev mode when
# requests reach uvicorn directly (bypassing nginx). In production,
# nginx replaces X-Forwarded-For with the real client IP ($remote_addr),
# so spoofing is not exploitable.
